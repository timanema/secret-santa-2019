plugins {
    id 'java'
}

group 'net.santa'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
}

jar {
    manifest {
        attributes 'Main-Class': 'net.santa.secretsanta.checker.Bootstrap'
    }
}

shadowJar {
    archiveFileName = "ss-checker.jar"

    doLast {
        copy {
            from 'build/libs/ss-checker.jar'
            into '../build/libs'
        }
    }

    onlyIf {
        true
    }
}